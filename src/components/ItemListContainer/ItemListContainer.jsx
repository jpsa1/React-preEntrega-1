import React from 'react'

const ItemListContainer = (props) => {

    return (
        <h4 style={{color:'lightblue', fontSize:'50px', textAlign:'center'}}>{props.saludo}</h4>
    )
}

export default ItemListContainer